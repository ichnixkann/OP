interface:
  display_name: "OP"
  short_description: "Repo-specific patterns and workflows for OP"
  default_prompt: "Use the OP repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true